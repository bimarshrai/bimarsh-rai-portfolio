'use client';
import Image from 'next/image';import Link from 'next/link';import {motion} from 'framer-motion';import type {Story} from '@/components/vow/site';
export default function StoryCard({story}: {story:Story}){return <Link href={'/stories/'+story.slug} className="group block focus-ring"><motion.div whileHover={{y:-5}} transition={{duration:.35}}><div className="relative aspect-[4/5] overflow-hidden"><Image src={story.images[0]} alt={`${story.title} concept story`} fill sizes="(max-width:768px) 90vw, 42vw" className="object-cover transition-transform duration-700 group-hover:scale-[1.04]"/></div><div className="pt-5 flex justify-between gap-5"><div><p className="eyebrow opacity-60">Illustrative project · {story.place}</p><h3 className="serif text-4xl mt-2 capitalize">{story.title}</h3></div><span className="text-xl">↗</span></div></motion.div></Link>}
