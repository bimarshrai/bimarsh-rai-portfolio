'use client';
import Image from 'next/image';import {useEffect} from 'react';import {AnimatePresence,motion} from 'framer-motion';import {X,ChevronLeft,ChevronRight} from 'lucide-react';
export default function Lightbox({images,index,onClose,onChange}:{images:string[];index:number|null;onClose:()=>void;onChange:(n:number)=>void}){useEffect(()=>{if(index===null)return;const f=(e:KeyboardEvent)=>{if(e.key==='Escape')onClose();if(e.key==='ArrowRight')onChange((index+1)%images.length);if(e.key==='ArrowLeft')onChange((index-1+images.length)%images.length)};addEventListener('keydown',f);return()=>removeEventListener('keydown',f)},[index,images.length,onChange,onClose]);return <AnimatePresence>{index!==null&&<motion.div className="fixed inset-0 z-[80] bg-[#0e0d0c]/95 p-5 md:p-10 flex items-center justify-center" initial={{opacity:0}} animate={{opacity:1}} exit={{opacity:0}} role="dialog" aria-modal="true" aria-label="Image viewer"><button className="absolute right-5 top-5 text-white focus-ring" onClick={onClose} aria-label="Close"><X/></button><button className="absolute left-5 top-1/2 text-white focus-ring" onClick={()=>onChange((index-1+images.length)%images.length)} aria-label="Previous"><ChevronLeft/></button><div className="relative w-full max-w-5xl h-[78vh]"><Image src={images[index]} alt="VOW concept wedding detail" fill className="object-contain" sizes="90vw"/></div><button className="absolute right-5 top-1/2 text-white focus-ring" onClick={()=>onChange((index+1)%images.length)} aria-label="Next"><ChevronRight/></button><p className="absolute bottom-5 left-1/2 -translate-x-1/2 text-white/60 eyebrow">{index+1} / {images.length} · Concept image</p></motion.div>}</AnimatePresence>}
